# OCR 机械修复报告

- 模式：已应用到文件
- 总替换次数：**216**
- 涉及文件数：16
- 备份文件：同目录下 `*.md.bak`（如需回退执行 `python3 ocr_fix.py --restore`）

## 分文件统计

### 04-第4章-反馈伺服系统.md（共 7 处）

- `辙入 → 输入` × **2**
- `雨个 → 两个` × **2**
- `辙出 → 输出` × **1**
- `考虚 → 考虑` × **1**
- `佴是 → 但是` × **1**

### 05-第5章-不互相影响的控制.md（共 14 处）

- `愉出 → 输出` × **3**
- `考虚 → 考虑` × **3**
- `辙出 → 输出` × **2**
- `雨个 → 两个` × **2**
- `输人 → 输入` × **2**
- `臂如说 → 譬如说` × **1**
- `辙入 → 输入` × **1**

### 06-第6章-交流与振荡控制伺服系统.md（共 9 处）

- `考虚 → 考虑` × **3**
- `辙出 → 输出` × **2**
- `输人 → 输入` × **2**
- `辙入 → 输入` × **1**
- `雨个 → 两个` × **1**

### 07-第7章-采样伺服系统.md（共 9 处）

- `辙入 → 输入` × **3**
- `考虚 → 考虑` × **3**
- `辙出 → 输出` × **1**
- `愉出 → 输出` × **1**
- `遒 → 道` × **1**

### 08-第8章-有时滞的线性系统.md（共 10 处）

- `反喂 → 反馈` × **2**
- `愎杂 → 复杂` × **1**
- `以过的 → 以前的` × **1**
- `科学莱 → 科学家` × **1**
- `臂如说 → 譬如说` × **1**
- `希果 → 如果` × **1**
- `辙出 → 输出` × **1**
- `考虚 → 考虑` × **1**
- `雨个 → 两个` × **1**

### 09-第9章-平稳随机输入下的线性系统.md（共 24 处）

- `辙入 → 输入` × **5**
- `考虚 → 考虑` × **5**
- `辙出 → 输出` × **4**
- `输人 → 输入` × **4**
- `佴是 → 但是` × **2**
- `遒 → 道` × **2**
- `氟流 → 气流` × **1**
- `雨个 → 两个` × **1**

### 10-第10章-继电器伺服系统.md（共 28 处）

- `辙出 → 输出` × **12**
- `辙入 → 输入` × **2**
- `运勤 → 运动` × **2**
- `酋先 → 首先` × **2**
- `输人 → 输入` × **2**
- `愎杂 → 复杂` × **1**
- `臂如说 → 譬如说` × **1**
- `继谨器 → 继电器` × **1**
- `愉出 → 输出` × **1**
- `遗嘁 → 遗憾` × **1**
- `考虚 → 考虑` × **1**
- `佴是 → 但是` × **1**
- `雨个 → 两个` × **1**

### 11-第11章-非线性系统.md（共 9 处）

- `辙出 → 输出` × **4**
- `辙入 → 输入` × **2**
- `臂如说 → 譬如说` × **1**
- `愉出 → 输出` × **1**
- `遒 → 道` × **1**

### 12-第12章-变系数线性系统.md（共 4 处）

- `臂如说 → 譬如说` × **1**
- `运勤 → 运动` × **1**
- `弹遒 → 弹道` × **1**
- `遒 → 道` × **1**

### 13-第13章-利用摄动理论的控制设计.md（共 16 处）

- `雨个 → 两个` × **4**
- `辙出 → 输出` × **2**
- `弹遒 → 弹道` × **2**
- `考虚 → 考虑` × **2**
- `辙入 → 输入` × **1**
- `扰劝 → 扰动` × **1**
- `正说弹道 → 正规弹道` × **1**
- `手绩 → 手续` × **1**
- `闽素 → 因素` × **1**
- `输人 → 输入` × **1**

### 14-第14章-满足指定积分条件的控制设计.md（共 5 处）

- `考虚 → 考虑` × **2**
- `愎杂 → 复杂` × **1**
- `辙出 → 输出` × **1**
- `雨个 → 两个` × **1**

### 15-第15章-自动寻求最优运转点的控制系统.md（共 34 处）

- `辙入 → 输入` × **15**
- `辙出 → 输出` × **12**
- `输人 → 输入` × **3**
- `愉出 → 输出` × **2**
- `考虚 → 考虑` × **1**
- `雨个 → 两个` × **1**

### 16-第16章-噪声过滤的设计原理.md（共 14 处）

- `辙入 → 输入` × **5**
- `辙出 → 输出` × **2**
- `佴是 → 但是` × **2**
- `雨个 → 两个` × **2**
- `反喂 → 反馈` × **1**
- `酋先 → 首先` × **1**
- `考虚 → 考虑` × **1**

### 17-第17章-自行镇定和适应环境的系统.md（共 9 处）

- `遒 → 道` × **2**
- `愎杂 → 复杂` × **1**
- `臂如说 → 譬如说` × **1**
- `重婴 → 重要` × **1**
- `觐点 → 观点` × **1**
- `考虚 → 考虑` × **1**
- `3动 → 自动` × **1**
- `雨个 → 两个` × **1**

### 18-第18章-误差的控制.md（共 23 处）

- `辙出 → 输出` × **9**
- `辙入 → 输入` × **7**
- `输人 → 输入` × **5**
- `雨个 → 两个` × **2**

### 19-参考文献与索引.md（共 1 处）

- `李雅遒 诺夫 → 李雅普诺夫` × **1**
