# Brain v3 / MACCS v2 — 扫码笔记(批次 1 弹药库)

> **目的**:为重写 `docs/README.md`(单一权威入口)和 9 份子系统设计稿提供"代码现状"的真实证据。
> **方法**:每条记录带 `file:line` 引用,基于 grep + Read 直接核对,不二手转述。
> **范围**:本批扫了 sdk/kernel + sdk/loop + sdk/protocol + cmd/brain + brains/ + 配置层。
> **生成时间**:2026-05-02
> **下一步**:用本笔记写 `docs/README.md` 和 9 份子文档重写。

---

## 1. 仓库布局速查

```
brain-v3/
├── cmd/brain/                 # CLI / HTTP 主入口(~30 .go 文件)
│   ├── chat/                  # 交互式 REPL 子包
│   ├── command/               # CLI 子命令
│   ├── config/                # 配置层(MACCSConfig 等)
│   └── dispatcher.go          # 16 个顶级子命令分发
├── sdk/                       # 28 个子包,全部代码
│   ├── kernel/   (~80 .go)    # 编排核心:Orchestrator/PlanOrch/ClosedLoop
│   ├── loop/     (~22 .go)    # Run 主循环 + Turn + Compressor
│   ├── llm/      (9 .go)      # Provider 抽象 + Anthropic/OpenAI/Mock
│   ├── protocol/ (8 .go)      # JSON-RPC 帧 + 方法名常量
│   ├── tool/     (~70 .go)    # 工具实现 + Registry + Sandbox
│   ├── agent/                 # Kind 类型定义
│   ├── events/                # EventBus
│   ├── flow/                  # Workflow / Edge
│   ├── observability/         # Span / Trace
│   ├── persistence/           # SQLite WAL 持久化
│   ├── runtimeaudit/          # 审计日志
│   ├── security/              # 权限/沙箱
│   ├── sidecar/               # sidecar 端 RPC
│   ├── executionpolicy/       # 执行策略
│   ├── license/               # 授权/Ed25519 签名
│   ├── toolguard/ toolpolicy/ # 工具白名单
│   ├── netutil/ shared/ test/ testing/ ...
│   └── docs/                  # 子系统设计稿(本次批次重写对象)
├── brains/                    # 8 个专精大脑
│   ├── browser/  code/  data/  desktop/
│   ├── easymvp/  fault/  quant/  verifier/
│   └── hooks.example.json
├── central/                   # 中央大脑文档
├── shared/docs/               # 跨脑共享设计
├── docs/                      # MACCS 顶级文档(本次重写主战场)
├── scripts/                   # 构建/发布脚本
├── npm/brain-cli/             # npm 包装层
└── go.mod / version.go / VERSION.json / CLAUDE.md / README.md / ...
```

---

## 2. 8 个真实大脑(brains/ 下)

| Kind | 路径 | 入口 | 类型 |
|------|------|------|------|
| `browser` | `brains/browser/` | `main.go` + `learner.go` | 进程内或 sidecar |
| `code` | `brains/code/` | `main.go` + `learner.go` | 进程内或 sidecar |
| `data` | `brains/data/` | `main.go` + `cmd/brain-data-sidecar/` | **独立 sidecar 二进制** |
| `desktop` | `brains/desktop/` | `main.go` + `learner.go` | 进程内或 sidecar |
| `easymvp` | `brains/easymvp/` | `cmd/brain-easymvp-sidecar/` | **独立 sidecar 二进制** |
| `fault` | `brains/fault/` | `main.go` + `learner.go` | 进程内或 sidecar |
| `quant` | `brains/quant/` | `main.go` + `cmd/brain-quant-sidecar/` | **独立 sidecar 二进制** |
| `verifier` | `brains/verifier/` | `main.go` + `learner.go` | 进程内或 sidecar |

> 每个 brain 目录顶级有 `brain.json` 写明 `kind` 字段。
> `data/quant/easymvp` 三个有独立 cmd/ 子目录单独编译为 sidecar 二进制(对应 CLAUDE.md 所述编译目标)。

---

## 3. CLI 子命令(cmd/brain/dispatcher.go)

`cmd/brain/dispatcher.go:18-35` 注册的 **16 个顶级子命令**:

| 命令 | 描述 | 实现文件 |
|------|------|---------|
| `chat` | 交互式 REPL | `cmd_brain.go::runChat` → `chat/` 子包 |
| `run` | 单次执行 | `cmd_run.go::runRun` |
| `status` | 查询 Run 状态 | `cmd_status.go::runStatus` |
| `resume` | 恢复中断的 Run | `cmd_resume.go::runResume` |
| `cancel` | 取消 Run | `cmd_cancel.go::runCancel` |
| `list` | 列出所有 Run | `cmd_list.go::runList` |
| `logs` | 查看 Run 日志 | `cmd_logs.go::runLogs` |
| `replay` | 审计回放 | `cmd_replay.go::runReplay` |
| `tool` | 管理工具注册表 | `cmd_tool.go::runTool` |
| `config` | 管理配置 | `cmd_config.go::runConfig` |
| `serve` | 启动 HTTP/WebSocket 服务 | `cmd_serve.go::runServe` |
| `brain` | 管理已安装大脑 | `cmd_brain.go::runBrainManage` |
| `pattern` | UI 模式库导入导出 | `cmd_pattern.go::runPattern` |
| `demo` | 人类示范序列管理 | `cmd_demo.go::runDemo` |
| `doctor` | 环境诊断 | `cmd_doctor.go::runDoctor` |
| `version` | 版本信息 | `cmd_version.go::runVersion` |

---

## 4. HTTP API 路由(cmd/brain/cmd_serve.go)

**已实地在 cmd_serve.go 找到的真实路由**(grep 行号确认):

### 系统类
| 方法 | 路径 | 行号 | 说明 |
|------|------|-----|------|
| GET | `/health` | 952 | 简易 liveness |
| GET | `/v1/readiness` | 959 | 启动期就绪检查(7 项 ProductionReadiness) |
| GET | `/v1/health` | 978 | HealthManager 持续监控 |
| GET | `/v1/version` | 1052 | 版本信息 |
| GET | `/v1/tools` | 1064 | 工具注册表 |
| GET | `/v1/brains` | 1088 | 已注册大脑列表 |

### 可观测 / 性能
| 方法 | 路径 | 行号 | 说明 |
|------|------|-----|------|
| GET | `/v1/metrics/perf` | 1000 | PerfCollector P50/P95/P99 |
| GET | `/v1/observability` | 1026 | ObservabilityHub Span 查询(可 ?trace_id 过滤) |

### Run / Execution(双入口同源)
| 方法 | 路径 | 行号 | 说明 |
|------|------|-----|------|
| POST/GET | `/v1/runs` | 1125 | 创建/列出 Run(别名) |
| GET/DELETE/POST | `/v1/runs/{id}` 及 `/interrupt` | 1139 | 查询/取消/中断 |
| POST/GET | `/v1/executions` | 1172 | v3 规范入口 |
| GET/POST | `/v1/executions/{id}` 及 `/stop` `/events` | 1186 | 查询/停止/SSE 事件流 |

### Job / Workflow / Plan / Project
| 方法 | 路径 | 行号 | 说明 |
|------|------|-----|------|
| POST/GET | `/v1/jobs` | 1221 | 后台任务 |
| GET | `/v1/jobs/{id}` | 1231 | 单 job 查询 |
| POST/GET | `/v1/workflows` | 1251 | DAG 工作流 |
| (在 cmd_serve_plans.go) | `/v1/plans` | — | PlanOrchestrator 入口 |
| (在 cmd_serve_projects.go) | `/v1/projects` | — | ClosedLoopController 入口 |

### Contract / Chaos
| 方法 | 路径 | 行号 | 说明 |
|------|------|-----|------|
| POST | `/v1/contracts/execute` | 1115 | 直接 contract 执行(SSE 流式) |
| POST/DELETE | `/v1/chaos/experiments` | (在 cmd_serve_chaos.go) | 混沌注入实验 |
| GET | `/v1/chaos/history` | 同上 | 历史实验记录 |

> `cmd_serve.go:613-614` 注释明确"三入口同流":
> 1. `/v1/runs/executions` → 单 Run
> 2. `/v1/plans` → `PlanOrchestrator` → `ExecuteTaskPlan`
> 3. `/v1/projects` → `ClosedLoopController` → `ExecuteTaskPlan`

---

## 5. 协议方法常量(sdk/protocol/methods.go)

| 常量 | 字符串值 | 方向 | 含义 |
|------|---------|-----|------|
| `MethodInitialize` | `initialize` | host→sidecar | 握手 |
| `MethodShutdown` | `shutdown` | host→sidecar | 优雅关停 |
| `MethodHeartbeat` | `heartbeat` | 双向 | 保活 |
| `MethodLLMComplete` | `llm.complete` | sidecar→host | 非流式补全 |
| `MethodLLMStream` | `llm.stream` | sidecar→host | 流式补全 |
| `MethodLLMStreamDelta` | `llm/stream/delta` | host→sidecar(notify) | 流式 delta 推送 |
| `MethodLLMRequestDirect` | `llm.requestDirectAccess` | sidecar→host(可选) | 请求直接访问 LLM |
| `MethodToolInvoke` | `tool.invoke` | sidecar→host | 工具调用 |
| `MethodPlanCreate` | `plan.create` | central→host | 持久化计划 |
| `MethodPlanUpdate` | `plan.update` | central→host | 追加计划事件 |
| `MethodArtifactPut` | `artifact.put` | sidecar→host | 上传产物 |
| `MethodArtifactGet` | `artifact.get` | sidecar→host | 下载产物 |
| `MethodSubtaskDelegate` | `subtask.delegate` | central→host | 派发子任务 |
| `MethodSpecialistCallTool` | `specialist.call_tool` | sidecar→host | 让专家调主进程工具 |
| `MethodBrainMetrics` | `brain/metrics` | sidecar→host | 上报性能指标 |
| `MethodHumanRequestTakeover` | `human/request_takeover` | sidecar→host | 请求人工接管 |
| `MethodBrainProgress` | `brain/progress` | sidecar→host(notify) | 进度 |
| `MethodBrainStreamWrite` | `brain/stream/write` | sidecar→host(notify) | 流式输出 |
| `MethodTraceEmit` | `trace.emit` | sidecar→host(notify) | 追踪事件 |
| `MethodAuditEmit` | `audit.emit` | sidecar→host(notify) | 审计事件 |
| `MethodProgressReport` | `progress/report` | sidecar→host(notify) | 进度汇报(MACCS 1.6) |
| `MethodProgressQuery` | `progress/query` | host→sidecar/内部 | 进度查询(MACCS 1.6) |
| `MethodInterruptSend` | `interrupt/send` | host-内部 / central→host | 中断信号(MACCS 1.3) |

---

## 6. Orchestrator(sdk/kernel/orchestrator.go)— 核心入口类

### 公开 API 表面
- `OrchestratorConfig` (line 78)
- `Orchestrator` struct (line 84) — 含 reviewLoop / chaos / perf / observability / leaseManager / capMatcher / learning / semanticApprover 等字段
- `OrchestratorOption` (line 264) + `With*` 函数:
  - `WithContextEngine` (268)
  - `WithCapabilityMatcher` (276)
  - `WithLearningEngine` (284)
  - `WithSemanticApprover` (292)
  - `WithLeaseManager` (301)
  - `WithMCPBrainPool` (310)
  - `WithChaosEngine` (320)
  - `WithPerfCollector` (335)
  - `WithObservability` (344)
  - **`WithReviewLoop` (353)** — Wave 7 0139b5e 新增

### 关键运行方法
| 方法 | 行号 | 用途 |
|------|-----|------|
| `Delegate(ctx, req)` | 749 | 单次派发 |
| `DelegateBatch(ctx, batch)` | 642 | 批次派发(layer 内并行) |
| `delegateOnce` | 905 | 派发实际执行体 |
| `CallTool(ctx, req)` | 1175 | 专家反向调主进程工具 |
| `ExecuteTaskPlan` (orchestrator_plan.go:38) | — | 执行 TaskPlan(分层并行) |
| `resolveTargetKind` | 1591 | 路由决策(combined = 0.4*cap + 0.25*learn + 0.35*causal) |
| `recordDelegateOutcome` | 1746 | 派发结果学习 |
| `assessActiveLearning` | 1813 | 主动学习不确定性评估 |
| `sendBrainLearn` | 1879 | 反向发学习信号给 sidecar |
| `HandleSubtaskDelegate` | 1915 | RPC handler 注册 |
| `HandleSpecialistCallTool` | 1927 | 同上 |

### 启动相关
- `AutoStartBrains` (469)
- `StartBrain(kind)` (484)
- `StopBrain(kind)` (496)
- `ListBrains` (525)
- `Register(reg)` (562) / `Unregister` (574)
- `CanDelegate` (599)
- `AvailableKinds` (617)

---

## 7. PlanOrchestrator(sdk/kernel/plan_orchestrator.go)

- `PlanOrchestrator` (line 16) — 含 ReviewLoop / MetaCognitive / DynamicBudget / MemoryRetriever / ContextEngine / ModelRouter / PatternExtractor / ExperienceStore / ProjectMemory 等字段
- `PlanOrchestratorConfig` (line 44)
- `NewPlanOrchestrator(orch, cfg)` (line 76)
- `consumeFeedbackRequests(ctx)` (159) — Wave 7/0139b5e 新增订阅 `brain.feedback.requested`
- **`ExecuteProject(ctx, plan)` (210)** — 主入口
- `runPatternExtraction(exp)` (361) — 异步模式提取(MACCS 5.4)

---

## 8. ClosedLoopController(sdk/kernel/closed_loop_controller.go)

- `ClosedLoopDeliverer` interface (15)
- `ClosedLoopRetrospector` interface (20)
- `ClosedLoopConfig` (25) / `NewDefaultClosedLoopConfig` (35)
- `ClosedLoopDeps` (43) — 含 Parser / Designer / Reviewer / Scheduler / Tester / DeliveryGen / Retrospect / SessionStore / Orchestrator
- `ClosedLoopResult` (70)
- `ClosedLoopController` (81)
- **`Execute(ctx, projectName, goal)` (98)** — 主入口,执行 7 阶段
- `runPhase` (395) / `advanceSM` (419) / `skipPhase` (429) / `notify` (437) / `failResult` (444)

> 7 阶段:Requirement → Design → Review → Schedule → Test → Delivery → Retrospect。

---

## 9. Runner(sdk/loop/runner.go)— LLM 主循环

- `Runner` struct (27)
- **`Execute(ctx, run, msgs, opts)` (164)** — 主循环
- `dispatchTools` (530) → `dispatchToolsSerial` (538) / `dispatchToolsBatched` (553)
- `executeSingleTool` (650)
- `consumeStream` (513)
- `buildChatRequest` (475)

> **MACCS 0.1 task_complete 终止**:line 397 `if strings.HasSuffix(tb.ToolName, ".task_complete")` 检测后终止 run。

---

## 10. 配置层(cmd/brain/config/config.go)

### 顶级字段(20-53 行)
| 字段 | 默认 | 用途 |
|------|------|------|
| `Mode` | — | 权限模式(open/restricted) |
| `Endpoint` | — | serve 模式监听地址 |
| `DefaultBrain` | — | 默认大脑 |
| `DefaultModel` | — | 默认模型 |
| `Output` | — | 输出格式 |
| `LogLevel` | — | 日志级别 |
| `Diagnostics` | — | 诊断配置 |
| `Timeout` | — | 单 turn 超时 |
| `Budget` | — | 默认预算 |
| `ChatMode` | — | chat 模式行为 |
| `PermissionMode` | — | 工具权限 |
| `ServeWorkdirPolicy` | — | serve 模式 workdir 策略 |
| `APIKey` / `BaseURL` / `Model` | — | LLM 单 provider 简写 |
| `Providers` / `ActiveProvider` | — | 多 provider 支持 |
| `Sandbox` | — | 沙箱配置 |
| `FilePolicy` | — | 文件策略(allow_read/create/edit/deny) |
| `ToolProfiles` / `ActiveTools` | — | 工具 profile |
| **`MACCS *MACCSConfig`** | — | MACCS v2 总开关 |

### MACCSConfig 子块(58-95 行)
| 字段 | 关联 Wave | 默认 |
|------|----------|------|
| `Health` (6.1) | Wave 6 | enabled=true |
| `Perf` (6.3) | Wave 6 | enabled=true |
| `Observability` (6.4) | Wave 6 | enabled=true |
| `Security` (6.5) | Wave 6 | enabled=true, reject_severity="high" |
| `MultiProject` (6.6) | Wave 6 | max_concurrent=3, queue_size=16 |
| `AdaptivePrompt` (5.5) | Wave 5 | enabled=true |
| `Conflict` (4.2/4.5) | Wave 4 | enabled=true, dry_run=true |
| `PatternExtractor` (5.4) | Wave 5 | enabled=true |
| **`Deadlock` (4.3/4.4)** | **Wave 7** | **enabled=true, dry_run=true** |

---

## 11. 学习系统组件(sdk/kernel/)— 真实文件清单

| 文件 | 类型/职责 | MACCS Wave |
|------|----------|-----------|
| `learning.go` | LearningEngine + RecordSequence + RecommendOrder | L1/L2 主体 |
| `causal_learning.go` | CausalLearner + Observe + LearnRelations + QueryEffect | 5.1 |
| `transfer_learning.go` | DefaultTransferLearner + (lang,domain,kind) 三元组 | 5.2 |
| `active_learning.go` | AssessUncertainty + EventBus brain.feedback.requested | 5.3 |
| `pattern_extraction.go` | PatternExtractor + Extract + AddPattern | 5.4 |
| `adaptive_prompt.go` | AdaptivePromptManager + RegisterVariant + SelectVariant | 5.5 |
| `capability_profile.go` | 能力画像可视化(Dashboard 展示) | 5.6 |
| `meta_cognitive.go` | MetaCognitive + Reflect + Lessons + Recommendations | 2.4 |
| `memory_retrieval.go` | MemoryRetriever 4 维加权(关键词+tag+时间衰减+重要度) | 2.2 |
| `complexity_estimator.go` | ComplexityEstimator(三段决策 learning→transfer→heuristic) | 2.3 |
| `model_router.go` | ModelRouter + Resolve + SyncToLLMProxy | 2.6 |

---

## 12. 编排/并发组件(sdk/kernel/)

| 文件 | 类型/职责 | MACCS Wave |
|------|----------|-----------|
| `task_plan.go` | TaskPlan + PlanSubTask + PlanTaskResult(含 Review 字段 1.10) | 1.1 |
| `project_progress.go` | ProjectProgress + ProgressStore | 1.2 |
| `interrupt.go` | InterruptSignal | 1.3 |
| `progress_rpc.go` | progress_report/query 双 RPC handler | 1.6 |
| `progress_store.go` | MemoryProgressStore(SQLite) | 1.7 |
| `dynamic_budget.go` | DynamicBudgetPool + Allocate + Reclaim | 1.9 |
| `review_loop.go` | ReviewLoopController + SubmitReview + ExecuteWithReview | 1.10 |
| `requirement_parser.go` | DefaultRequirementParser | 3.2 |
| `design_api.go` | DefaultDesignGenerator | 3.3 |
| `design_review.go` | DesignReviewLoop | 3.5 |
| `execution_scheduler.go` | ExecutionScheduler + RunPlan + AttachConflictControl + **AttachDeadlockControl(Wave 7)** | 3.6 / 4.x |
| `acceptance_tester.go` | DefaultAcceptanceTester(exec.CommandContext 真跑) | 3.7 |
| `delivery_generator.go` | DefaultDeliveryGenerator | 3.8 |
| `retrospective.go` | RetrospectiveEngine | 3.9 |
| `closed_loop_controller.go` | 7 阶段编排器 | 3.10 |
| `conflict_detector.go` | DefaultConflictDetector + Detect + DetectPair | 4.2 |
| `deadlock_detector.go` | DeadlockDetector + AddWaitEdge + Detect + SuggestVictim | 4.3 |
| `arbiter.go` | DefaultArbiter + ResolveDeadlock + 5 种策略 | 4.4 |
| `smart_scheduler.go` | SmartScheduler + Reschedule(贪心冲突分离) | 4.5 |
| `lease.go` | MemLeaseManager + AcquireSet(canonical-order) | 4.1 |

---

## 13. 生产硬化组件(sdk/kernel/)

| 文件 | 类型/职责 | MACCS Wave |
|------|----------|-----------|
| `health_check.go` | HealthManager + brainPool/leaseManager checker | 6.1 |
| `chaos_engine.go` | ChaosEngine + DelayInjector + ErrorInjector | 6.2 |
| `perf_benchmark.go` | PerfCollector(P50/P95/P99 按 brain.kind 分桶) | 6.3 |
| `observability.go` | ObservabilityHub + TraceSpan(内存 provider) | 6.4 |
| `security_audit.go` | DefaultSecurityAuditor + ValidateInput | 6.5 |
| `multi_project.go` | MultiProjectManager(MaxConcurrent/QueueSize) | 6.6 |
| `production_readiness.go` | NewReadinessChecker + 7 项 check | 6.7 |

---

## 14. Loop 层关键(sdk/loop/)

- `runner.go` — LLM 主循环(见 §9)
- `turn_executor.go` / `turn.go` / `turn_helper.go` — Turn 执行
- `compressor.go` — 上下文压缩
- `interrupt.go` — Loop 层中断检查(MACCS 1.4)
- `checkpoint.go` — Checkpoint(MACCS 1.5,含 PlanID/CurrentTaskID/ProjectID 字段)
- `batch_planner.go` — Welsh-Powell 着色(B-1)
- `cache.go` / `mem_cache.go` — 工具结果缓存
- `loop_detector.go` / `mem_loop_detector.go` — 死循环检测
- `sanitizer.go` / `mem_sanitizer.go` — 输入清洗
- `stream.go` / `mem_stream.go` — 流式
- `tool_observer.go` — 工具观察者

---

## 15. LLM Provider(sdk/llm/)

- `provider.go` — Provider interface
- `anthropic_provider.go` — Anthropic 实现(MACCS 0.2 90s→180s 超时)
- `openai_provider.go` — OpenAI 实现
- `mock_provider.go` — 测试用 mock
- `chat_request.go` / `chat_response.go` — 请求响应类型
- `usage.go` — Token 用量
- `response_validation.go` — 响应校验
- `http_error.go` — HTTP 错误处理

---

## 16. Tool 层(sdk/tool/)— ~70 个文件

### 内置工具(builtin_*.go)
- `builtin_read_file.go` / `builtin_write_file.go` / `builtin_edit_file.go` / `builtin_delete_file.go` / `builtin_list_files.go`
- `builtin_search.go` / `builtin_shell_exec.go` / `builtin_task_complete.go` / `builtin_reject_task.go`
- `builtin_note.go` / `builtin_memory.go` / `builtin_echo.go`
- `builtin_browser*.go` — 浏览器系列(snapshot, network, sitemap, anomaly, pattern, understand, visual, multimodal 等)
- `builtin_desktop.go` / `builtin_fault.go` / `builtin_verifier.go`

### Registry / Sandbox / 框架
- `registry.go` / `mem_registry.go` — 工具注册表
- `tool.go` — Tool interface 与基础类型
- `sandbox.go` / `sandbox_tool.go` — 沙箱
- `command_exec.go` / `command_sandbox{_linux,_darwin,_windows,_other}.go` — 命令执行沙箱
- `retry.go` — 重试策略
- `risk.go` — 风险评估
- `output_schema.go` / `schema.go` — 输出 schema
- `failure_log.go` / `failure_log_wrapper.go` — 失败日志
- `human_takeover.go` — 人工接管
- `sequence_recorder.go` — 序列记录(L2 学习数据源)
- `semantic_cache.go` / `semantic_rules.go` — 语义缓存

### Browser 专精(深度)
- `ui_pattern*.go` — UI 模式库(13 个文件,含学习、匹配、IO、auth、ecommerce 等)
- `anomaly_*.go` — 异常感知
- `cdp/` — Chrome DevTools Protocol

---

## 17. 待重写的 9 份子文档对应代码区域(供下批次使用)

| 子文档 | 对应代码 | MACCS 增强点 |
|-------|---------|-------------|
| `32-v3-Brain架构.md` | sdk/ 全部 | 编排层(PlanOrch/ClosedLoop)需补充 |
| `29-第三方专精大脑开发.md` | brains/ + sdk/sidecar | 模型差异化配置(model_router) |
| `35-BrainCapability标签与匹配算法.md` | sdk/kernel/capability.go + orchestrator.go:1591 | causalScore 0.35 加权 |
| `35-BrainPool实现设计.md` | sdk/kernel/pool.go pool_entry.go pool_health.go | 冲突仲裁(ExecutionScheduler) |
| `35-Context-Engine详细设计.md` | sdk/kernel/context_engine.go context_engine_memory.go | 项目级记忆 + LLM 摘要 |
| `35-Dispatch-Policy-冲突图与Batch分组算法.md` | sdk/kernel/conflict_detector.go smart_scheduler.go | ConflictDetector + SmartScheduler |
| `35-LeaseManager实现设计.md` | sdk/kernel/lease.go | Wave 7 解释:不动 LM,从 ConflictDetector 路径接入死锁 |
| `35-跨脑通信协议设计.md` | sdk/protocol/methods.go(本笔记 §5) | progress.report/query + brain.feedback.requested |
| `35-自适应学习L1-L3算法设计.md` | sdk/kernel/learning.go + causal/transfer/active/pattern/adaptive_prompt | 因果/迁移/主动/模式/Prompt 五项升级 |

---

## 18. 关键事实速查(写入口文档时常用)

- **接入率**: **48/48 = 100%**(0139b5e 5 项审计差距 + Wave 7 4.3/4.4 接入完成)
- **架构层级**: L3(中央) → L2(8 大脑) → L1(工具) → L0(学习)
- **3 个执行入口同源**: `/v1/runs` / `/v1/plans` / `/v1/projects` 都最终调 `Orchestrator.ExecuteTaskPlan`
- **协议**: JSON-RPC 2.0 over stdio / HTTP / WebSocket(三种 transport)
- **持久化**: SQLite WAL @ `~/.brain/brain.db`(Phase E 统一)
- **远程双模式**: stdio(本地 sidecar) + HTTP/WebSocket BidirRPC(远程)
- **学习层**: L0(brain 内特化) + L1(EWMA + Bandit) + L2(序列学习) + L3(LLM 记忆) + 因果/迁移/主动/模式/Prompt 五项 v2 增强
- **并发安全网**: Lease(canonical-order 防 lock 死锁) + Conflict(批前检测) + Deadlock(wait-for graph) + Arbiter(victim 选择)
- **可观测**: Trace span / Perf P50-P99 / Health checker / Audit log / Readiness check
- **8 大脑**: code, browser, verifier, fault, data, quant, desktop, easymvp

---

## 19. 下批次执行清单

**批次 2(BrainPool + LeaseManager + Dispatch-Policy)**:
- 读 `sdk/kernel/pool.go` `pool_entry.go` `pool_health.go` 完整文件
- 读 `sdk/kernel/lease.go` 完整文件(本批已读)
- 读 `sdk/kernel/conflict_detector.go` `smart_scheduler.go` 完整文件
- 重写 `sdk/docs/35-BrainPool实现设计.md`
- 重写 `sdk/docs/35-LeaseManager实现设计.md`
- 重写 `sdk/docs/35-Dispatch-Policy-冲突图与Batch分组算法.md`

**批次 3(智能层 4 份)**:
- 读 `context_engine.go` `context_engine_memory.go` `memory_retrieval.go`
- 读 `learning.go` `causal_learning.go` `transfer_learning.go` `active_learning.go` `pattern_extraction.go` `adaptive_prompt.go`
- 读 `progress_rpc.go` 验证协议侧 progress 路由
- 读 `capability.go` + orchestrator.go:1591 周边
- 重写 4 份子文档

**批次 4(架构 + 第三方 + 入口终稿)**:
- 读 `sdk/sidecar/` 全部 + `brains/*/main.go`
- 重写 `32-v3-Brain架构.md` + `29-第三方专精大脑开发.md`
- 写 `docs/README.md` 终稿(本批已有骨架雏形)
- 删除 `docs/MACCS-文档索引.md`(被 docs/README.md 取代)
- 同步 `sdk/docs/README.md` 删除已 git rm 的条目
