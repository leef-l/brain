# 数据库与配置规范

> Schema · YAML 配置 · 品种映射 · 数据保留策略

**上级文档**: [35-量化系统三脑架构总览](35-量化系统三脑架构总览.md)

---

## 一、PostgreSQL Schema

### 1.1 已有表（brain-trader-v3-gpt5/schema.sql）

```sql
CREATE SCHEMA IF NOT EXISTS trader;
SET search_path TO trader;

-- K线数据
CREATE TABLE IF NOT EXISTS candles (
    inst_id   VARCHAR(32) NOT NULL,
    bar       VARCHAR(8)  NOT NULL,
    ts        BIGINT      NOT NULL,
    o         DOUBLE PRECISION NOT NULL,
    h         DOUBLE PRECISION NOT NULL,
    l         DOUBLE PRECISION NOT NULL,
    c         DOUBLE PRECISION NOT NULL,
    vol       DOUBLE PRECISION NOT NULL,
    vol_ccy   DOUBLE PRECISION,
    PRIMARY KEY (inst_id, bar, ts)
);
CREATE INDEX IF NOT EXISTS idx_candles_ts ON candles (bar, ts DESC);

-- 特征向量
CREATE TABLE IF NOT EXISTS vectors (
    id         BIGSERIAL PRIMARY KEY,
    collection VARCHAR(32) NOT NULL,
    inst_id    VARCHAR(32) NOT NULL,
    timeframe  VARCHAR(8),
    ts         BIGINT NOT NULL,
    vector     BYTEA NOT NULL,
    metadata   JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_vectors_lookup
    ON vectors (collection, inst_id, timeframe, ts DESC);

-- 向量标签（监督学习）
CREATE TABLE IF NOT EXISTS vector_labels (
    vector_id     BIGINT PRIMARY KEY REFERENCES vectors(id) ON DELETE CASCADE,
    ret_5m        DOUBLE PRECISION,
    ret_15m       DOUBLE PRECISION,
    ret_1h        DOUBLE PRECISION,
    ret_4h        DOUBLE PRECISION,
    ret_24h       DOUBLE PRECISION,
    max_up_24h    DOUBLE PRECISION,
    max_down_24h  DOUBLE PRECISION,
    labeled_at    TIMESTAMPTZ DEFAULT NOW()
);

-- 交易记录
CREATE TABLE IF NOT EXISTS trades (
    id              BIGSERIAL PRIMARY KEY,
    account_id      VARCHAR(32) NOT NULL DEFAULT 'default',
    trace_id        VARCHAR(64),
    mode            VARCHAR(8) NOT NULL,
    inst_id         VARCHAR(32) NOT NULL,
    direction       VARCHAR(8) NOT NULL,
    leverage        INT NOT NULL,
    entry_price     DOUBLE PRECISION NOT NULL,
    exit_price      DOUBLE PRECISION,
    quantity        VARCHAR(32) NOT NULL,
    pnl_pct         DOUBLE PRECISION,
    pnl_usdt        DOUBLE PRECISION,
    hold_seconds    INT,
    strategy        VARCHAR(32) NOT NULL,
    entry_reason    TEXT,
    exit_reason     VARCHAR(32),
    max_profit_pct  DOUBLE PRECISION,
    max_loss_pct    DOUBLE PRECISION,
    entry_vector_id BIGINT REFERENCES vectors(id),
    opened_at       TIMESTAMPTZ NOT NULL,
    closed_at       TIMESTAMPTZ,
    created_at      TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_trades_time ON trades (opened_at DESC);
CREATE INDEX IF NOT EXISTS idx_trades_strategy ON trades (strategy, opened_at DESC);
CREATE INDEX IF NOT EXISTS idx_trades_account ON trades (account_id, opened_at DESC);

-- 策略每日统计
CREATE TABLE IF NOT EXISTS strategy_daily (
    strategy    VARCHAR(32) NOT NULL,
    date        DATE NOT NULL,
    mode        VARCHAR(8) NOT NULL,
    account_id  VARCHAR(32) NOT NULL DEFAULT 'default',
    trades      INT NOT NULL DEFAULT 0,
    wins        INT NOT NULL DEFAULT 0,
    pnl_pct     DOUBLE PRECISION DEFAULT 0,
    sharpe      DOUBLE PRECISION,
    max_dd      DOUBLE PRECISION,
    weight      DOUBLE PRECISION,
    PRIMARY KEY (strategy, date, mode, account_id)
);

-- 系统状态 KV
CREATE TABLE IF NOT EXISTS system_state (
    key         VARCHAR(64) PRIMARY KEY,
    value       JSONB NOT NULL,
    updated_at  TIMESTAMPTZ DEFAULT NOW()
);
```

### 1.2 新增表

```sql
-- ========================
-- 宏观事件
-- ========================
CREATE TABLE IF NOT EXISTS macro_events (
    id           BIGSERIAL PRIMARY KEY,
    event_id     VARCHAR(64) UNIQUE NOT NULL,  -- "us_nfp_202604"
    name         VARCHAR(128) NOT NULL,         -- "Non-Farm Payrolls"
    country      VARCHAR(8) NOT NULL,           -- "US"
    importance   VARCHAR(8) NOT NULL,           -- "high", "medium", "low"
    scheduled_at TIMESTAMPTZ NOT NULL,
    actual       DOUBLE PRECISION,
    forecast     DOUBLE PRECISION,
    previous     DOUBLE PRECISION,
    surprise     DOUBLE PRECISION,              -- actual - forecast
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_macro_scheduled
    ON macro_events (scheduled_at DESC);

-- ========================
-- 新闻事件
-- ========================
CREATE TABLE IF NOT EXISTS news_events (
    id           BIGSERIAL PRIMARY KEY,
    source       VARCHAR(32) NOT NULL,          -- "reuters", "coindesk"
    title        TEXT NOT NULL,
    summary      TEXT,
    sentiment    DOUBLE PRECISION,              -- [-1, 1]
    symbols      TEXT[],                         -- {"BTC", "ETH"}
    tags         TEXT[],                         -- {"fed", "hawkish"}
    url          TEXT,
    published_at TIMESTAMPTZ NOT NULL,
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_news_time
    ON news_events (published_at DESC);

-- ========================
-- 链上事件
-- ========================
CREATE TABLE IF NOT EXISTS chain_events (
    id           BIGSERIAL PRIMARY KEY,
    chain        VARCHAR(16) NOT NULL,          -- "ethereum", "bitcoin"
    event_type   VARCHAR(32) NOT NULL,          -- "whale_transfer", "exchange_inflow"
    amount_usd   DOUBLE PRECISION,
    from_type    VARCHAR(32),                   -- "whale", "exchange", "unknown"
    to_type      VARCHAR(32),
    tx_hash      VARCHAR(128),
    occurred_at  TIMESTAMPTZ NOT NULL,
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_chain_time
    ON chain_events (occurred_at DESC);

-- ========================
-- 数据质量告警
-- ========================
CREATE TABLE IF NOT EXISTS validator_alerts (
    id           BIGSERIAL PRIMARY KEY,
    level        VARCHAR(16) NOT NULL,          -- "warning", "critical"
    alert_type   VARCHAR(32) NOT NULL,          -- "price_spike", "gap", "stale"
    symbol       VARCHAR(32),
    detail       TEXT,
    event_ts     TIMESTAMPTZ,
    created_at   TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_validator_alerts_time
    ON validator_alerts (created_at DESC);

-- ========================
-- 信号审计追踪
-- ========================
CREATE TABLE IF NOT EXISTS signal_traces (
    id                  BIGSERIAL PRIMARY KEY,
    trace_id            VARCHAR(64) UNIQUE NOT NULL,
    symbol              VARCHAR(32) NOT NULL,
    snapshot_seq        BIGINT NOT NULL,
    price               DOUBLE PRECISION NOT NULL,
    direction           VARCHAR(8),
    confidence          DOUBLE PRECISION,
    dominant_strategy   VARCHAR(32),
    global_risk_allowed BOOLEAN,
    global_risk_reason  TEXT,
    account_results     JSONB,          -- 各账号处理结果
    features            BYTEA,          -- 192维向量（压缩）
    signals_json        JSONB,          -- 各策略原始信号
    created_at          TIMESTAMPTZ DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_signal_traces_time
    ON signal_traces (created_at DESC);
CREATE INDEX IF NOT EXISTS idx_signal_traces_symbol
    ON signal_traces (symbol, created_at DESC);

-- ========================
-- 账号每日快照
-- ========================
CREATE TABLE IF NOT EXISTS account_snapshots (
    id           BIGSERIAL PRIMARY KEY,
    account_id   VARCHAR(32) NOT NULL,
    date         DATE NOT NULL,
    equity       DOUBLE PRECISION NOT NULL,
    pnl_day      DOUBLE PRECISION,
    pnl_pct      DOUBLE PRECISION,
    trades       INT DEFAULT 0,
    wins         INT DEFAULT 0,
    max_dd       DOUBLE PRECISION,
    sharpe       DOUBLE PRECISION,
    created_at   TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE (account_id, date)
);

-- ========================
-- 每日复盘报告
-- ========================
CREATE TABLE IF NOT EXISTS daily_reviews (
    id                BIGSERIAL PRIMARY KEY,
    date              DATE UNIQUE NOT NULL,
    input_json        JSONB NOT NULL,
    analysis_json     JSONB NOT NULL,
    actions_json      JSONB,
    actions_executed  BOOLEAN DEFAULT FALSE,
    review_duration_ms INT,
    created_at        TIMESTAMPTZ DEFAULT NOW()
);
```

## 二、数据保留策略

| 表 | 保留时间 | 清理方式 |
|---|---------|---------|
| `candles` (1m) | 60 天 | 定时 DELETE WHERE bar='1m' AND ts < cutoff |
| `candles` (5m+) | 永久 | — |
| `vectors` | 60d / 180d（按配置） | 定时 DELETE |
| `vector_labels` | 跟随 vectors | CASCADE |
| `trades` | 永久 | — |
| `strategy_daily` | 永久 | — |
| `macro_events` | 永久 | — |
| `news_events` | 90 天 | 定时 DELETE |
| `chain_events` | 30 天 | 定时 DELETE |
| `validator_alerts` | 30 天 | 定时 DELETE |
| `signal_traces` | 30 天 | 定时 DELETE |
| `account_snapshots` | 永久 | — |
| `daily_reviews` | 永久 | — |
| `system_state` | 永久 | — |

清理建议在 Central Brain 的每日复盘后执行。

## 三、完整配置 YAML 规范

```yaml
# ============================================================
# 量化系统完整配置
# ============================================================

# 全局模式
mode: paper                            # paper | live

# 交易品种（留空 = 全量采集 OKX 所有 SWAP 品种）
# 指定品种则只采集这些
instruments: []

# ============================================================
# 数据大脑配置
# ============================================================
data:
  # 活跃品种列表 — 控制哪些品种持久化到 PG
  active_list:
    min_volume_24h: 10000000     # 最低 24h 成交额（USDT）
    max_instruments: 100          # 最大品种数
    update_interval: 168h         # 更新间隔（7天）
    # 强制包含的品种（即使成交额不够也持久化）
    always_include:
      - BTC-USDT-SWAP
      - ETH-USDT-SWAP
      - SOL-USDT-SWAP

  providers:
    - name: okx_main
      type: okx_swap
      params:
        ws_url: wss://ws.okx.com:8443/ws/v5/public
        instruments: []            # 空 = 全量订阅所有 SWAP 品种
        reconnect_delay: [1, 2, 4, 8, 16, 30]
        history_candles: 500

    # 回测模式替换为:
    # - name: backtest
    #   type: replay
    #   params:
    #     from: "2025-01-01"
    #     to: "2025-12-31"
    #     speed: 0

    # 未来扩展:
    # - name: us_macro
    #   type: fred_macro
    #   params:
    #     api_key: ${FRED_API_KEY}
    #     series: [UNRATE, CPIAUCSL, FEDFUNDS]
    #     poll_interval: 1h
    #
    # - name: econ_calendar
    #   type: investing_cal
    #   params:
    #     importance: [high]
    #     countries: [US, CN]
    #     poll_interval: 5m
    #
    # - name: reuters_news
    #   type: news_websocket
    #   params:
    #     url: wss://news-feed.example.com
    #     topics: [crypto, fed, macro]
    #
    # - name: eth_chain
    #   type: chain_eth
    #   params:
    #     rpc_url: ${ETH_RPC_URL}
    #     watch_addresses: [exchange_hot_wallets]
    #     whale_threshold_usd: 1000000

  # 品种映射
  instruments:
    normalize:
      okx:
        BTC-USDT-SWAP: BTC-USDT-SWAP
        ETH-USDT-SWAP: ETH-USDT-SWAP
        SOL-USDT-SWAP: SOL-USDT-SWAP
      # binance:
      #   BTCUSDT: BTC-USDT-PERP
      #   ETHUSDT: ETH-USDT-PERP

  # 数据质量规则
  validation:
    max_price_change_pct: 10.0
    max_future_ts_ms: 5000
    max_stale_ts_ms: 300000
    gap_backfill_threshold: 3

# ============================================================
# 策略配置
# ============================================================
strategy:
  initial_weights: [0.30, 0.25, 0.25, 0.20]   # Trend, Mean, Breakout, OrderFlow
  weight_update_interval: 24h
  weight_min: 0.10
  weight_max: 0.40
  weight_temperature: 2.0
  signal_threshold: 0.45

# ============================================================
# 特征向量配置
# ============================================================
vector:
  dimensions: 192
  hnsw_m: 16
  hnsw_ef_construction: 200
  hnsw_ef_search: 100
  pattern_retention_1m: 60d
  pattern_retention_5m: 180d

# ============================================================
# 全局风控（跨账号）
# ============================================================
global_risk:
  max_global_exposure_pct: 50.0
  max_global_same_direction: 30.0
  max_global_daily_loss: 5.0
  max_symbol_exposure: 15.0

# ============================================================
# 默认风控（per-account 可覆盖）
# ============================================================
risk:
  max_position_pct: 5.0
  max_leverage: 20
  max_concurrent: 5
  max_total_exposure: 30.0
  max_same_direction: 20.0
  daily_loss_pause: 3.0
  daily_loss_close: 5.0
  circuit_breaker_volatility: 99
  circuit_breaker_btc_move: 5.0

# ============================================================
# LLM 审查配置
# ============================================================
llm:
  enabled: true
  model: haiku
  trigger_concurrent: 3
  trigger_position_pct: 5.0
  trigger_daily_loss: 3.0
  timeout: 10s

# ============================================================
# 多账号配置
# ============================================================
accounts:
  - id: main_live
    label: "主账号-实盘"
    enabled: true
    mode: live
    exchange: okx
    executor:
      backend: okx_ws
      okx_api_key: ${OKX_MAIN_API_KEY}
      okx_secret: ${OKX_MAIN_SECRET}
      okx_passphrase: ${OKX_MAIN_PASSPHRASE}
    weight_factor: 1.0
    equity_limit: 50000

  - id: sub_conservative
    label: "子账号-保守"
    enabled: true
    mode: live
    exchange: okx
    executor:
      backend: okx_ws
      okx_api_key: ${OKX_SUB1_API_KEY}
      okx_secret: ${OKX_SUB1_SECRET}
      okx_passphrase: ${OKX_SUB1_PASSPHRASE}
    weight_factor: 0.5
    risk:
      max_leverage: 10
      max_concurrent: 3
    allowed_strategies:
      - TrendFollower
      - BreakoutMomentum

  - id: paper_test
    label: "模拟盘-全策略"
    enabled: true
    mode: paper
    exchange: okx
    executor:
      backend: paper
      paper_slippage: 0.0003
      paper_maker_fee: 0.0002
      paper_taker_fee: 0.0005
    weight_factor: 1.0

# ============================================================
# 数据库
# ============================================================
database:
  host: localhost
  port: 5432
  dbname: brain_trader
  user: trader
  password: ${PG_PASSWORD}
  sslmode: disable
  schema: trader
  max_conns: 10
  min_conns: 1
  conn_max_idle_time: 30m
  conn_max_lifetime: 2h
```

## 四、品种映射表

### 4.1 当前支持

| 标准化 ID | OKX 原始 | 类型 |
|-----------|---------|------|
| BTC-USDT-SWAP | BTC-USDT-SWAP | 永续合约 |
| ETH-USDT-SWAP | ETH-USDT-SWAP | 永续合约 |
| SOL-USDT-SWAP | SOL-USDT-SWAP | 永续合约 |

### 4.2 未来扩展示例

| 标准化 ID | 交易所 | 原始 ID |
|-----------|--------|--------|
| BTC-USDT-PERP | Binance | BTCUSDT |
| BTC-USDT-PERP | Bybit | BTCUSDT |
| 600519-CNY-STOCK | A股 | 600519.SH |
| SPY-USD-ETF | 美股 | SPY |

### 4.3 标准化规则

```
格式: {BASE}-{QUOTE}-{TYPE}

TYPE 取值:
  SWAP   — 永续合约（OKX 命名）
  PERP   — 永续合约（Binance/Bybit 命名）
  SPOT   — 现货
  STOCK  — 股票
  ETF    — ETF
  FUTURE — 交割合约（含到期日: BTC-USDT-FUTURE-20260630）
```

## 五、环境变量清单

| 变量 | 用途 | 必填 |
|------|------|------|
| `PG_PASSWORD` | PostgreSQL 密码 | 是 |
| `OKX_MAIN_API_KEY` | 主账号 API Key | live 模式 |
| `OKX_MAIN_SECRET` | 主账号 Secret | live 模式 |
| `OKX_MAIN_PASSPHRASE` | 主账号 Passphrase | live 模式 |
| `OKX_SUB1_API_KEY` | 子账号 1 API Key | 按需 |
| `OKX_SUB1_SECRET` | 子账号 1 Secret | 按需 |
| `OKX_SUB1_PASSPHRASE` | 子账号 1 Passphrase | 按需 |
| `FRED_API_KEY` | 美联储数据 API Key | Phase 4 |
| `ETH_RPC_URL` | 以太坊 RPC 节点 | Phase 4 |
| `ANTHROPIC_API_KEY` | LLM API Key | 审查+复盘 |
